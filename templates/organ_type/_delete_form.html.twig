<form method="post" action="{{ path('organ_type_delete', {'id': organ_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organ_type.id) }}">
    <button class="btn">Delete</button>
</form>
