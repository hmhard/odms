<form method="post" action="{{ path('donation_center_delete', {'id': donation_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donation_center.id) }}">
    <button class="btn btn-danger float-right">Delete</button>
</form>
