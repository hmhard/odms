<form method="post" action="{{ path('donor_delete', {'id': donor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donor.id) }}">
    <button class="btn btn-danger float-right">Delete</button>
</form>
