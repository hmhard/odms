{% extends 'base.html.twig' %}

{% block title %}Edit Donation{% endblock %}
{% block maintitle %}Edit Donation{% endblock %}

{% block body %}
  	<div class="card">

		<div class="card-header">
          <a class="btn btn-primary float-right" href="{{ path('donation_index') }}">Back</a>
		
			<div class="card-tools">
			
			</div>
		</div>
		<div class="card-body">
    {{ include('donation/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('donation/_delete_form.html.twig') }}
{% endblock %}
